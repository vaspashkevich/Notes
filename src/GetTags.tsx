import { useNote } from "./NoteLayout";

const note = useNote()

export function getTags( event: any ) {
        // const inputValue = event.target.value;
        // const newTags = inputValue
        // inputValue.split(' ')
        //   note.markdown.filter((note.markdown) => note.markdown.startsWith('#'))
        //   .map((tag) => tag.slice(1))
          return <h1></h1>
      }
